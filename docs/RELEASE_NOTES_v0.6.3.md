# rest-sample-cache-reader 0.6.3

`0.6.3` aligns the runnable reader with `rust-java-rest:4.4.1` and
`java-rust-cache:0.7.3`. The packaged native runtime now uses exact full-commit provenance.

Reader routes, projection contracts, Redis topology settings, and Java business code are unchanged.
